# Industrial Surface Defect Segmentation (U-Net)

Train a U-Net segmentation model using synthetic metal textures + synthetic defects.
Includes class imbalance simulation, illumination augmentation, and label noise.